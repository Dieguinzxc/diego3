function setup()}
 createCanvas(400, 400);
 background("white");
}
 function draw() {
strok("black");
fill("red");
//console.log(mouseIsPressed);
if(mouseIsPressed){
rect(mouseX, mouseY, 20, 35);
}
 }
